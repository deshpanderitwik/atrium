// Arrive holds at most three declared tasks.
//
// The cap is the point, not a convenience. An unbounded strip drifts back into
// a computed list — the app reporting on you rather than you stating something
// — and the choosing quietly stops. Three forces the choosing.
export const STAR_LIMIT = 3;
